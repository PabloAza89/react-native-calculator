// MyCustomScrollView.kt
//package com.yourpackage.views // Use your actual package name
package com.reactnativecalculator

import android.content.Context
import com.facebook.react.views.scroll.ReactScrollView
import com.facebook.react.uimanager.ThemedReactContext

import android.widget.LinearLayout
import android.view.View
import android.view.ViewGroup 

//import com.facebook.react.views.scroll.VirtualViewContainerState

// Extend ReactScrollView to inherit all standard ScrollView behavior
//class MyCustomScrollView(context: Context) : ReactScrollView(context) {
class MyCustomScrollView(context: Context) : ReactScrollView(context) {

    // A private field to hold the custom prop value
    //private var customPropValue: String = ""
    // init {
    //     // Set the property on the Android ScrollView
    //     //this.clipToPadding = false 
    //     mVirtualViewContainerState = null
    // }

    // fun setCustomProp(value: String) {
    //     //this.customPropValue = value
    //     // 🔑 Apply the logic for your custom prop here
    //     // Example: Log the value or change a property of the ScrollView
    //     //println("CustomScrollView received customProp: $customPropValue")

    //     // If you need to change a native property, do it here.
    //     // Example: if (value == "red") setBackgroundColor(Color.RED)
    // }
    //private var mVirtualViewContainerState: VirtualViewContainerState? = null
    // You can add more methods here to be called from the View Manager
    // override fun getVirtualViewContainerState() {
    //     if (mVirtualViewContainerState == null) {
    //         mVirtualViewContainerState = VirtualViewContainerState.create(this)
    //     }
    //     return mVirtualViewContainerState!!
    // }
}
