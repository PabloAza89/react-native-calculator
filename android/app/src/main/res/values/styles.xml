<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:windowBackground">@drawable/splashscreen</item> <!-- ORI -->
        <!-- <item name="android:windowBackground">@drawable/ic_launcher_foreground</item> --> <!-- TEST -->
        <!-- <item name="android:windowBackground">@drawable/ic_launcher_background</item> --> <!-- TEST -->
        <!-- <item name="android:windowFullscreen">true</item> -->
        <item name="android:statusBarColor">#A22FE0</item> <!-- RED --> <!-- 2: STATUS BAR -->
        <item name="android:navigationBarColor">#DCE04F</item> <!-- YELLOW --> <!-- 2: NAVIGATION BAR -->
        <!-- <item name="android:windowDisablePreview">true</item> --> <!-- TEST -->
        <!-- <item name="android:windowIsTranslucent">true</item> --> <!-- TEST -->
        <!-- <item name="android:windowSplashScreenBackground">@color/test</item> --> <!-- TEST --> <!-- LIGHT TURQUOISE -->
        <!-- <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item> --> <!-- TEST -->
    </style>

    <!-- <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style> -->

</resources>
